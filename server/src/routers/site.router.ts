import express from 'express';

import * as SitesController from '../app/controllers/product.controller';
const router = express.Router();

//router.get('/', SitesController.show);

export default router;
